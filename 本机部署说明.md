# 影策 open-ai-canvas · 本机 Docker 部署说明（2026-08-30）

## 一、访问地址

| 场景 | 地址 |
|---|---|
| 本机 | http://localhost:3100 |
| 局域网（主网段 en0） | http://192.168.9.148:3100 |
| 局域网（副网卡 en1） | http://192.168.10.29:3100 |
| **公网（Cloudflare 隧道，自带 HTTPS）** | **https://yingce.cc.cd** |

> 端口用 **3100** 而不是默认 3000，因为 3000 已被 new-api 占用。
> 同一局域网内的其他电脑/手机，浏览器直接打开上面的局域网 IP 即可使用。

## 二、首次使用（重要）

1. 浏览器打开 http://localhost:3100
2. **第一个注册的账号自动成为管理员**（当前 users 表为空，firstUser=true）
3. 用户名 3-32 位字母/数字/下划线/连字符，密码至少 8 位，首个账号**不需要邮箱**
4. 注册后公开注册自动保持关闭（CANVAS_REGISTRATION_ENABLED=false），其他人无法自行注册
5. 登录后在「设置 / 系统渠道」里配置模型 Base URL、API Key、模型名才能生成图片/视频

## 三、部署结构

- 部署目录：`~/open-ai-canvas`（在 Home 卷 APFS；外置盘 /Volumes/5T 是 ExFAT，git/数据库都不稳，所以没放那）
- 编排：`docker-compose.deploy.yml`（运行）+ `docker-compose.build.yml`（本地构建镜像）
- 5 个服务：postgres:17、redis:7.4、migrate（一次性数据库迁移任务，跑完 Exited(0) 属正常）、backend(Go)、web(Nginx)
- 当前版本：**v1.2.3.1**（2026-08-31 从 v1.2.0-preview.1 更新；schema v3）
- **本地定制**：`backend/internal/protocol/builtin.go` 里手工加了「红鸟 HongNiao」图像/视频渠道适配（未提交，git 显示 M）。每次 `git pull` 上游未改该文件会自动保留；若上游改了同文件需手动合并，补丁备份在 `~/open-ai-canvas-backups/hongniao-adapter-*.patch`
- 对外只暴露 web 的 3100；backend 8080 / postgres / redis 只在 compose 内网
- **数据全部用 Docker 命名卷**（存在 Docker 虚拟机磁盘内，绕开 ExFAT 锁问题）：
  - open-ai-canvas_postgres-data（主数据库）
  - open-ai-canvas_redis-data
  - open-ai-canvas_backend-data（上传的媒体素材 /data）
- 重启策略 unless-stopped + Docker Desktop 开机自启 → Mac 重启后服务自动恢复

## 四、构建时的网络处理（复现用）

- Docker Hub 直连超时，基础镜像通过 `docker.m.daocloud.io` 拉取后改回标准标签
- Go 模块走 `GOPROXY=https://goproxy.cn,direct`（已写进 .env / build 参数）
- 前端 bun 依赖直连 registry.npmjs.org 成功，未改 Dockerfile

## 五、常用运维命令

```bash
cd ~/open-ai-canvas
DC="docker compose --env-file .env -f docker-compose.deploy.yml -f docker-compose.build.yml"

$DC ps                      # 查看状态（4 个都应 healthy）
$DC logs -f backend         # 跟随后端日志
$DC restart backend         # 重启后端
$DC down                    # 停止（数据保留在命名卷）
$DC up -d                   # 启动
$DC up -d --build backend web   # 改代码后重新构建并启动
```

## 六、数据库备份与恢复

```bash
# 备份（输出到 Home 卷，别放外置盘，launchd/TCC 会拦）
mkdir -p ~/open-ai-canvas-backups
docker exec open-ai-canvas-postgres-1 pg_dump -U open_ai_canvas -d open_ai_canvas --no-owner \
  | gzip > ~/open-ai-canvas-backups/yingce-db-$(date +%Y%m%d-%H%M%S).sql.gz

# 恢复
gzip -dc ~/open-ai-canvas-backups/xxx.sql.gz \
  | docker exec -i open-ai-canvas-postgres-1 psql -U open_ai_canvas -d open_ai_canvas
```

## 七、更新到最新版（已验证流程）

### 最简单：双击 `更新影策.command`

访达进入 `~/open-ai-canvas`，**双击 `更新影策.command`** 即可（自动打开终端跑下面全流程，结束按任意键关闭）。它等价于官方「在线热更新」，但适配本机：
- 自动备份数据库、保护红鸟定制、拉代码、构建、migrate 迁移、滚动重建、验证本机与公网
- 已是最新时直接退出，不重复构建；日志在 `~/open-ai-canvas-backups/update-*.log`
- 命令行等价：`bash ~/open-ai-canvas/update-yingce.sh`

> **为什么不用后台「系统更新/热更新」里的官方 Host Updater**：官方 `open-ai-canvas-host-updater` 只发布 **linux-amd64/arm64**，安装脚本硬性要求 Linux + systemd（macOS 没有，会直接报「仅支持 Linux 服务器」）；而且它是拉取官方发布镜像来替换，会**冲掉本地构建的红鸟适配**。所以本机用上面的脚本，后台那个页面显示「更新器未连接」属正常，忽略即可。

### 手动分步命令（脚本内部就是这套）

```bash
cd ~/open-ai-canvas
DC="docker compose --env-file .env -f docker-compose.deploy.yml -f docker-compose.build.yml"

# 1) 更新前先备份数据库（重要，schema 会迁移）
docker exec open-ai-canvas-postgres-1 pg_dump -U open_ai_canvas -d open_ai_canvas --no-owner \
  | gzip > ~/open-ai-canvas-backups/yingce-db-$(date +%Y%m%d-%H%M%S).sql.gz

# 2) 确认本地红鸟定制仍在（grep 应有输出），再拉代码
grep -c hongniao backend/internal/protocol/builtin.go
git pull --ff-only origin main

# 3) 重新构建镜像（含 migrate/backend/web；基础镜像本地已齐，Go 走 goproxy.cn）
$DC build migrate backend web

# 4) 滚动重建：migrate 先迁移 schema(Exited 0) → backend → web
$DC up -d --wait --remove-orphans

# 5) 验证：版本/迁移/公网
curl -s http://127.0.0.1:3100/api/health         # build.version 应为新版本，schema.ready=true
curl -s -A "Mozilla/5.0" -o /dev/null -w "%{http_code}\n" https://yingce.cc.cd/
```

- v1.2.3 起迁移由独立的 `migrate` 服务执行（backend 的 CANVAS_AUTO_MIGRATE=false），`ps -a` 里看到 migrate `Exited (0)` 是正常的
- 回滚：代码 `git checkout <旧commit>` 后重走 3-4；数据库用第 1 步的备份按第六节恢复

## 八、公网访问（已完成 · Cloudflare Tunnel）

公网地址：**https://yingce.cc.cd** （免费域名 yingce.cc.cd 来自 DNSHE，NS 托管到 Cloudflare）

**复用了 new-api 的同一条隧道和同一个 cloudflared 进程**（隧道 ID 20082b38），靠主机名分流，当前 ingress：

| 公网域名 | 转发到本机 | 服务 |
|---|---|---|
| a8api.cc.cd | http://localhost:3000 | new-api |
| yingce.cc.cd | http://127.0.0.1:3100 | 影策（本次） |

- cloudflared 由 launchd 托管：`~/Library/LaunchAgents/com.cloudflare.newapi-tunnel.plist`，token 在 `~/.cloudflared/newapi.token`，开机自启 + 崩溃自重启
- 新增域名的入口：Cloudflare Zero Trust → 网络 → Tunnels → 该隧道 → 公共主机名（Subdomain 留空、域名选 yingce.cc.cd、HTTP、127.0.0.1:3100）
- 公网验证必须带 UA，否则被 Cloudflare 拦 403/1010：`curl -A "Mozilla/5.0" https://yingce.cc.cd/api/health`
- 隧道运维：
  - 重启：`launchctl kickstart -k gui/$(id -u)/com.cloudflare.newapi-tunnel`
  - 状态：`launchctl print gui/$(id -u)/com.cloudflare.newapi-tunnel`
  - 日志：`tail -f ~/.cloudflared/tunnel.err`（ingress 配置、Registered connection 都在这里）

### 踩坑：隧道 token 被轮换会导致 530/1033「Invalid tunnel secret」

在 Cloudflare 后台查看/重装连接器时如果 token 被重新生成，本地旧 token 会失效，表现为**所有域名（含 new-api）一起 530、日志报 `Unauthorized: Invalid tunnel secret`**。修复：
1. 后台复制最新安装命令里的 token（`cloudflared service install` 后面那串）
2. 覆盖写入 `~/.cloudflared/newapi.token`（chmod 600，旧文件已自动备份为 *.bak.*）
3. `launchctl kickstart -k gui/$(id -u)/com.cloudflare.newapi-tunnel`
4. 日志出现 `Updated to new configuration` + `Registered tunnel connection` 即恢复

> 注意：macOS 上**不要**真的执行 `sudo cloudflared service install`，本机用 launchd 托管即可，重复装会冲突。

### 踩坑：反复 1033、日志「connection with edge closed / no recent network activity」——代理节点在跳

现象：本机服务正常，但公网间歇 1033，cloudflared 连上边缘几秒就被掐断、不断重连。根因是 **Clash 当前节点出口 IP 在多个 IP 间轮换/不稳**（cloudflared 需要一条长期不换的连接）。2026-08-31 定位过程：
- `curl https://www.cloudflare.com/cdn-cgi/trace` 连测几次，`ip=` 在两个 IP 间跳 = 节点在轮换
- 当时用的「2x专线-日本-3」延迟探测超时、连接秒断；http2/quic、单连接、锁定真实边缘 IP 都无效（直连真实边缘 IP 反而连不上，必须走代理）
- **解决：Clash Verge 里把主组「三毛机场」切到固定的稳定专线节点（当天用「2x专线-香港-3」，出口固定 95.40.222.226，边缘落到 hkg）**，再 `launchctl kickstart -k gui/$(id -u)/com.cloudflare.newapi-tunnel`
- 验证：trace 的 ip 连测不变；日志两条 Registered 后不再出现 terminated；公网连测 10/10 200
- 经验：**别用「♻️自动选择」这类会自动切换的组跑隧道**，固定一个稳定专线节点；订阅更新后若选择被重置，重新选一次即可
- Clash Verge(mihomo) 本地控制：unix socket `/tmp/verge/verge-mihomo.sock`，可读代理组/切节点（`curl --unix-socket <sock> http://localhost/proxies`）

## 九、关键文件

- `.env`：数据库随机密码、端口、运行开关（权限 600，已被 .gitignore 忽略）
- `build.log`：本次镜像构建日志

## 十、红鸟 AI（hongniaoai）渠道接入（2026-08-31）

- Base URL：`https://open.hongniaoai.com`（等价 `api.hongniaoai.com`），接口前缀 `/v1`；模型清单 `GET /v1/models`（Bearer key，返回自带价格）。清单留档：`hongniao-models.json`、`红鸟模型价格清单.md/.csv`。
- 影策是两层模型：**系统渠道模型**（`model_channels`+`channel_models`，含协议/价格/能力参数）→ **前台逻辑模型**（`logical_models`+`logical_model_routes`，创作端真正可见）。只建渠道模型不会出现在创作端，必须再建一层前台模型并绑路由；价格策略选「跟随供应价格(channel)」即沿用渠道模型的按次/按秒价。
- 已接入 6 个视频模型（渠道 `CHANNEL_000005`「红鸟AI」，协议 `hongniao-video`，1 积分按 1 元成本口径）：
  - 按次 fixed_request：`hnsd2-fast-933-720p`、`seedance2.0-9tu-manxue`，均 2.8 积分/次。
  - 按秒 per_second：`wan3-720p` 0.2/秒、`video-sd25-480p` 0.3/秒、`video-sd25-720p` 0.4/秒、`video-ds-2.5` 0.53/秒（总价=单价×秒数，报价接口已核对无误）。
- **坑 1（Clash fake-ip 导致建渠道报「不允许访问本机/内网地址」）**：TUN+fake-ip 把域名解析成 198.18.x / fdfe:dcba:: 假地址，被后端 SSRF 校验拦截；Docker 部署又无法用「本机渠道」豁免（要求 loopback 绑定+开关）。解法：在 `docker-compose.build.yml` 的 backend 加 `extra_hosts` 把域名钉到真实公网 IP（红鸟在 Cloudflare 后，DoH 取 `172.67.214.119`），再 `up -d --wait backend`。IP 失效时用 `https://dns.google/resolve?name=open.hongniaoai.com&type=A` 重新解析更新。
- **坑 2（手写适配器字段名不对，已修）**：`backend/internal/protocol/builtin.go` 的 `hongniaoVideosAdapter` 原来发 `duration/aspectRatio/audioUrls/videoUrls`，红鸟真实字段是 `seconds/aspect_ratio/audios/videos`（错字段会被静默忽略，导致用户选的时长/比例不生效）。已改正并重新 build backend；该文件是本地未提交定制，`git pull` 更新时同红鸟适配一起保留，更新后需重新 build。
- **前提：红鸟账户必须有余额**。实测当前余额 ¥0.00，参数正确时上游返回「余额不足，需要 ¥x」，影策侧表现为模型测试 502「模型服务拒绝了请求」。充值后即通；到时候用「后台→渠道→模型测试」或直接创作一条最便宜的（wan3 5 秒约 1 元）做端到端验证。
- 再加新模型：后台「渠道→红鸟AI→新增模型」填 modelKey=红鸟模型 id、协议 hongniao-video、能力 video、按次/按秒价、视频能力参数（时长 enum、比例、文生/图生），再到「前台模型」新建并把该渠道模型绑成供应线路、价格跟随供应。
